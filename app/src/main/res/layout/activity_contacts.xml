<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0f172a"
    android:padding="20dp">

    <!-- Title -->
    <TextView
        android:id="@+id/tvContactsTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Emergency Contacts"
        android:textColor="#ffffff"
        android:textSize="22sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginTop="40dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Card container for contact listings -->
    <LinearLayout
        android:id="@+id/contactsCard"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="#1e293b"
        android:padding="20dp"
        android:layout_marginTop="20dp"
        android:elevation="6dp"
        android:backgroundTint="#1e293b"
        app:layout_constraintTop_toBottomOf="@id/tvContactsTitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Contact 1 -->
        <TextView
            android:id="@+id/contact1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="• Primary Emergency Contact: 9876543210"
            android:textColor="#e2e8f0"
            android:textSize="16sp"
            android:paddingBottom="12dp"/>

        <!-- Contact 2 -->
        <TextView
            android:id="@+id/contact2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="• Police Emergency: (112)"
            android:textColor="#e2e8f0"
            android:textSize="16sp"
            android:paddingBottom="12dp"/>

        <!-- Contact 3 -->
        <TextView
            android:id="@+id/contact3"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="• Ambulance / Medical Emergency: (108)"
            android:textColor="#e2e8f0"
            android:textSize="16sp"/>
    </LinearLayout>

    <!-- Bottom Back Button -->
    <Button
        android:id="@+id/btnBackContacts"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Back"
        android:textAllCaps="false"
        android:backgroundTint="#2563eb"
        android:textColor="#ffffff"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/contactsCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
